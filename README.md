# oledctl
oledctl works as a middleman between brightnessctl and gammastep. This keeps gammastep's color temperature in sync with brightnessctl's brightness, so it feels like adjusting the same brightness brightnessctl reports. This is needed on most OLED displays, which lack a backlight and don't expose any way to change the pixel brightness directly.
